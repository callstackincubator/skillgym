# cleanup-install-versions template

Monorepo stub with inconsistent Node `engines` and duplicated `packageManager` fields.

The agent should align every `package.json` to the same Node floor and keep `packageManager` only at the repository root.
